#!/bin/sh
# Simple pre-commit hook used during development. Fails the commit if any
# linters or tests do not pass.
set -e

poetry run ruff check .           # linting and import sorting
poetry run mypy loopbloom         # static type analysis
PYTHONPATH=. poetry run pytest --cov=loopbloom --cov-fail-under=80 -q
