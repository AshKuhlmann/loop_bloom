#!/bin/sh
# Simple pre-commit hook used during development. Fails the commit if any
# linters or tests do not pass.
set -e

flake8                 # style checks
mypy loopbloom         # static type analysis
PYTHONPATH=. pytest --cov=loopbloom --cov-fail-under=80 -q
